{% block stylesheets %}
    <style>
        .btn {
            margin:10px;
            padding:7px ;
        }
    </style>
{% endblock %}
{% block body %}
<form method="post" action="{{ path('app_i_p_address_delete', {'id': i_p_address.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ i_p_address.id) }}">
    <button class="btn">Supprimer</button>
</form>
{% endblock %}