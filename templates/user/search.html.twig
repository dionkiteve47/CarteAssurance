<form action="{{ path('search_users') }}" method="get">
    <input type="text" name="searchq" id="searchq" placeholder="Chercher selon titre" style="font-size: 16px; padding: 8px;">
    <button type="submit" style="font-size: 16px; padding: 8px;">Search</button>
</form>
